# Day 3 Run Sheet (09:00-10:00 CET) - 2026-03-20

Use this sheet to execute the first hour of Day 3 consistently across the 10-agent experiment lane.

## Preflight (08:55-09:00)
1. Confirm branch and clean state:
```powershell
git status --short
git rev-parse --abbrev-ref HEAD
```
2. Pull latest main:
```powershell
git pull --ff-only origin main
```
3. Open required docs:
- docs/experiments/day3-assignment-matrix-2026-03-20.md
- docs/experiments/day3-replay-checklist-exp-001-exp-002.md
- docs/experiments/day3-end-of-day-decision-memo-2026-03-20.md

## 09:00-09:15 Kickoff Sync

### queen-ctoa
- Goal: freeze scope and enforce max 2 active experiments.
- Checks:
  - `EXP-001` replay stays active.
  - `EXP-002` rescope replay stays active.
  - no new experiment added.
- Output: kickoff decision note (single paragraph in team log).

### pm-roadmap
- Goal: lock ownership and deadlines for first hour.
- Commands:
```powershell
rg "^\|" docs/experiments/day3-assignment-matrix-2026-03-20.md
```
- Output: confirmed owner matrix with no blanks.

### qa-safety + ci-publisher
- Goal: confirm gate criteria for 10:00 checkpoint.
- Checks:
  - replay run evidence format fixed
  - required fields for go/hold/kill known
- Output: pass/fail criteria pinned for first replay batch.

## 09:15-09:35 Scope Lock and Replay Setup

### prompt-forge (EXP-001)
- Goal: lock replay prompt candidate and baseline pairing.
- Commands:
```powershell
rg "EXP-001|Prompt" docs/experiments/*.md
```
- Checks:
  - only one major variable changed (prompt pack)
  - task sample family consistent with Day 2
- Output: replay prompt set ready.

### tool-advisor + bot-architect (EXP-002)
- Goal: lock simplified routing variant.
- Commands:
```powershell
rg "EXP-002|Routing|complexity" docs/experiments/*.md
```
- Checks:
  - remove complexity-heavy branch from Day 2 challenger
  - keep baseline constant
- Output: rescoped routing variant ready.

### builder-engine
- Goal: create run log skeleton for replay evidence.
- Suggested table fields:
  - run_id
  - experiment_id
  - timestamp
  - baseline_or_challenger
  - task_sample_id
  - quality_score
  - operator_load
  - cycle_time
  - retry_count
  - safety_flag
- Output: run log template shared.

## 09:35-10:00 Replay Batch 1 Execution

### EXP-001 replay team
Agents: prompt-forge, lua-scripter, qa-safety
- Action:
  - execute replay batch 1
  - record quality, correctness, operator-load, safety
- Quick checks:
  - no safety regression
  - reproducibility signal present

### EXP-002 rescope team
Agents: tool-advisor, bot-architect, qa-safety
- Action:
  - execute rescoped routing replay batch 1
  - record retries, cycle-time, quality, complexity impact
- Quick checks:
  - no quality drift
  - complexity budget not exceeded

### mmo-intel
- Goal: add domain relevance checks to both batch outputs.
- Output: domain relevance note for batch 1.

### ci-publisher
- Goal: early promotion-lane feasibility check.
- Checks:
  - evidence structure complete enough for checkpointing
  - no policy blocker introduced
- Output: pre-check status for 10:00 review.

## 10:00 Micro-Checkpoint (Required)

1. EXP-001 status:
- continue / hold / kill (batch-1 provisional)

2. EXP-002 status:
- continue / hold / kill (batch-1 provisional)

3. Blockers:
- technical:
- process:

4. Next hour priorities (10:00-11:00):
- EXP-001:
- EXP-002:

## Fast Command Bundle
Run these quickly in sequence if needed:
```powershell
git status --short
git pull --ff-only origin main
rg "EXP-001|EXP-002|Day 3|Decision" docs/experiments/*.md
```

## Definition of Good First Hour
- Scope remained fixed.
- Replay batch 1 completed for both experiments.
- Evidence captured in consistent format.
- Provisional go/hold/kill recorded at 10:00.
