# CTOA Toolkit v1.1.1 Release Notes

Release date: 2026-05-24

## Added

- Docker workflow guard steps in [`.github/workflows/docker-build.yml`](.github/workflows/docker-build.yml):
  - ACR credentials and target input validation
  - Docker Hub token validation
  - VPS deploy secret validation
- VPS wrapper action `dashboard-snapshot` in [`deploy/vps/wrappers/ctoa-root-action.sh`](deploy/vps/wrappers/ctoa-root-action.sh)
- Final freeze validation evidence update in [`runtime/ci-artifacts/release-exception-dashboardsnapshot-2026-05-24.md`](runtime/ci-artifacts/release-exception-dashboardsnapshot-2026-05-24.md)

## Changed

- Docker build context hardening in [`.dockerignore`](.dockerignore): excludes runtime and operational artifacts (`runtime`, `logs`, `backups`, `archived`, `releases`) and tightens editor file exclusions.
- Release task chain in [`.vscode/tasks.json`](.vscode/tasks.json):
  - `CTOA: Check Update Gate` and `CTOA: Repo Hygiene Audit` use `python` invocation for Windows compatibility.
  - `CTOA: Release Ready Pass` now depends on active validator `CTOA: Sprint-042 Validate`.
  - Added `CTOA: Final Freeze (Local Exception: No DashboardSnapshot)` helper task.
- VPS Dashboard snapshot execution path in [`scripts/ops/ctoa-vps.ps1`](scripts/ops/ctoa-vps.ps1) now uses wrapper-based `sudo -n` call instead of legacy root shell path.

## Fixed

- Critical workflow expression corruption in [`.github/workflows/docker-build.yml`](.github/workflows/docker-build.yml) for publish/deploy gating.
- Release blockers on Windows shell tasks caused by `.venv/Scripts/python.exe` path handling in [`.vscode/tasks.json`](.vscode/tasks.json).
- VPS DashboardSnapshot failures from passworded sudo path by moving to wrapper action with NOPASSWD-friendly execution.

## Ops

Validation results:

- `CTOA: Run All Tests` -> PASS (`167 passed, 6 skipped`)
- `CTOA: Check Update Gate` -> PASS (`launch_allowed`)
- `CTOA: Validate Pack` -> PASS
- `CTOA: Sprint-042 Validate` -> PASS (`11/11 checks`)
- `CTOA: DashboardSnapshot OneShot` -> PASS

Docker KPI:

- Build context payload reduction (excluded operational artifacts): `2.246 MiB` (`2,355,014` bytes)

Release decision:

- Ready for release.
