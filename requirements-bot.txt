# AGENT 7: Tibia Bot dependencies
mss>=9.0.1          # Screen capture
opencv-python-headless>=4.10.0.84,<5.0.0.0  # Computer vision / state parsing
pyautogui>=0.9.54   # Input simulation
pydirectinput>=1.0.4  # DirectInput-safe key/mouse dispatch
pywin32>=306; platform_system == "Windows"  # Window integrations on Windows
pynput>=1.7.6       # Low-level input
numpy>=1.26         # Array ops for CV
torch>=2.2.0        # ML model (Phase 2)
fastapi>=0.111      # Dashboard API server
uvicorn[standard]>=0.30  # ASGI server for dashboard container
