# Test Loot Filter

## Cases
- blacklist always wins
- empty whitelist means allow-all mode
- stackable items are detected
- classify returns loot, stack, or skip
- missing count defaults to zero for partial items
